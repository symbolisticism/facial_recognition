import 'package:flutter/material.dart';

class IndividualSummaries extends StatelessWidget {
  const IndividualSummaries({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Individual Summaries'),
      ),
    );
  }
}
